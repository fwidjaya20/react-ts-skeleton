import { Route } from '@skeleton/shared/router/contracts/route.contract';

const routes: Route[] = [
	{
		path: '',
		component: 'modules/home/pages/home.page',
	},
];

export const HOME_ROUTES: Route[] = routes;
